/* Variáveis */
let idade = 26; //criando a primeira variável

console.log(idade);

/* Tipos de variáveis */

idade = 38; //Number
let numero = 39; //Number
let altura = 1.73; //Number
let nome = 'Gil'; //String (texto)
let estudando = true; //Booleano (true or false)

console.log(numero, typeof numero);
console.log(altura, typeof altura);
console.log(nome, typeof nome);
console.log(estudando, typeof estudando);

var variavel = 30; //recomenda-se utilizar o let ao invés do var

const constante = 20; //constante possui apenas o valor indicado e não pode ser alterada, diferente do let (variável)